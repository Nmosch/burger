
<h1>Eat-Da-Burger!</h1>

<ul>    
    {{#each burgers}}
        {{#unless devoured}}
        <li>
            {{burger_name}}
            <button type = "button" class = "devoured" data-id={{id}} >Devour</button>
        </li>
        {{/unless}}
    {{/each}}
</ul>

<ul>
    {{#each burgers}}
        {{#if devoured}}
        <li>
            {{burger_name}}
        </li>
        {{/if}}
    {{/each}}
</ul>

<form>
    <input id = "newBurger" type="text">
    <button id="addBurger">Add burger</button>
</form>